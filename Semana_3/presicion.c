#include <stdio.h>
#include <math.h>
int main(){
  int n;
  float Ea,x,a;
  for(i=0; i++; i=n){
    printf("Programa que calcula la serie de Mc Laurin para e^x\n");
    printf("tomando en cuenta el error aproximado")
    printf("ingresa el número de iteraciones: \n");
    scanf(&n);
    printf("ingresa la variable x\n")
    scanf(&x);
    pow(e,x)=A;
    (A-a)/A = Ea;
    printf("n= %d , e^x= , Ea=",&n ,&a, &Ea);


  }

  return 0;
}